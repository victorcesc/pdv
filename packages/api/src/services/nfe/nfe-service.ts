/**
 * Orquestração: validar -> calcular -> persistir -> emitir.
 * Será implementado após Tax calculator e gov-client.
 */
export async function createAndEmitNota(_payload: unknown): Promise<unknown> {
  return { id: null, status: "rascunho" };
}
